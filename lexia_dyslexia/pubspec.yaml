name: lexia_dyslexia
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.6.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Firebase packages - Using compatible versions
  firebase_core: ^3.12.1
  cloud_firestore: ^5.4.4 
  firebase_auth: ^5.5.1
  firebase_storage: ^12.4.4
  
  # UI packages
  google_fonts: ^6.1.0
  provider: ^6.0.5
  flutter_spinkit: ^5.2.0
  cached_network_image: ^3.3.0
  
  # Routing
  go_router: ^14.8.1
  
  # Utilities
  intl: ^0.20.2
  shared_preferences: ^2.2.0
  url_launcher: ^6.2.0
  flutter_markdown: ^0.7.6+2
  connectivity_plus: ^6.1.3  # Ensure this is included
  flutter_secure_storage: ^9.0.0
  get_it: ^8.0.3
  
  # The following adds the Cupertino Icons font to your application.plication.
  # Use with the CupertinoIcons class for iOS style icons.oIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of yourur
  # package. See that file for information about deactivating specific lintn about deactivating specific lint
  # rules and activating additional ones.g additional ones.
  flutter_lints: ^5.0.0 

# For information on the generic Dart part of this file, see theile, see the
# following page: https://dart.dev/tools/pub/pubspec# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.llowing section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons ination, so that you can use the icons in
  # the material Icons class..
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:d assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/fonts/

  # An image asset can refer to one or more resolution-specific "variants", seetion-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, seepackage dependencies, see
  # https://flutter.dev/to/asset-from-package  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,e,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with ah a
  # list giving the asset and other descriptors for the font. Foring the asset and other descriptors for the font. For
  # example:ple:
  fonts:
    - family: OpenDyslexic
      fonts:
        - asset: assets/fonts/OpenDyslexic-Regular.otf
        - asset: assets/fonts/OpenDyslexic-Bold.otf
          weight: 700
        - asset: assets/fonts/OpenDyslexic-Italic.otf
          style: italic 
  #
  # For details regarding fonts from package dependencies,endencies,
  # see https://flutter.dev/to/font-from-package  # see https://flutter.dev/to/font-from-package

